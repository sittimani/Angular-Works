import { MismatchDirective } from './mismatch.directive';

describe('MismatchDirective', () => {
  it('should create an instance', () => {
    const directive = new MismatchDirective();
    expect(directive).toBeTruthy();
  });
});
